# Session Handoff — 2026-06-02

> 給下一個 session 的接手筆記。本 session 在 d22 sweep 進行中、d12 DFS 重算自動觸發鏈布署完畢時切斷。
>
> 完整狀態見 [`DEEP_STATUS.md`](DEEP_STATUS.md)、TT 政策見 [`D12_TT_SWEEP.md`](D12_TT_SWEEP.md)、整體架構見 [`CLAUDE.md`](CLAUDE.md)。

## 1. 本 session 完成的事

| 動作 | commit |
|---|---|
| 棋譜 +385 d12 FEN，render + push 公開站 | `147f6a0` |
| 政策代碼：中貴只 d12、其他全 d22、\|d12\|>500 cutoff、d22 PV [:10] | `61ee228` |
| 註冊 schtask `ChessBookEnrichD22` 22:30 daily, 暫停 `ChessBookVerifyD28Shunbao` | — (Windows 操作) |
| positions_view.js 瘦身 52.6→25.9 MB（chessdb moves[] 拿掉、PV 截短） | `05672e4` |
| DEEP_STATUS.md 接進 render pipeline 自動同步 7 個欄位 | `4c29211` |
| **新 build_data.py DFS evaluator + Hash 1024 MB**（smoke test 通過） | `0d76b7d` |
| 自動觸發鏈：d22 sweep 清空時自動觸發 d12 DFS 全量重算 | `96c5300` |

## 2. 現在進行中

### 2.1 d22 sweep (ChessBookEnrichD22)
- 每晚 22:30 跑 8h，目前進度 24,208 / ~37,000 (約 41%)
- 第 1 夜跑完 9,157 FEN，3.1s/FEN
- 預估再 ~2-3 晚完成

### 2.2 自動觸發鏈（已布署，等發動）
`run_enrich_d22.ps1` 已加 `--auto-d12-recompute` 旗標。當 enrich_decisive：
- 跑完 todo 且未撞 deadline → **同夜**觸發 `recompute_d12_full.py`
- 隔夜啟動時 todo=0 → **隔夜**觸發（safety net）

`recompute_d12_full.py` 步驟：backup → 刪 positions.js → build_data.py -d 12 全量重算 → enrich_decisive 補邊界 → render → commit → push → drop marker `output/.d12_dfs_recompute_done`（one-shot 鎖）。

預期 d22 在第 3 晚（06-02 → 06-03）清空，**該夜同時觸發 d12 recompute（~75 min）**，master 06-03 早醒來看到完整結果。

### 2.3 暫停中
`ChessBookVerifyD28Shunbao` schtask **已 disabled**，避免 22:30 撞 d22。d22 + d12 recompute 全部完成後手動 `schtasks /Change /TN ChessBookVerifyD28Shunbao /ENABLE` 恢復。

## 3. 新 session 第一件事該做什麼

```powershell
# 1. 看 d22 sweep 狀態
Get-ChildItem output\enrich_d22_*.log | Sort LastWriteTime -Descending | Select -First 3
Get-Content (Get-ChildItem output\enrich_d22_*.log | Sort LastWriteTime -Desc | Select -First 1).FullName -Tail 30

# 2. 看 d12 recompute marker 是否已生成
Test-Path output\.d12_dfs_recompute_done

# 3. 看 SQLite 各 depth 當前 row 數
.\.venv\Scripts\python.exe site_builder\migrate_to_sqlite.py 2>&1 | Select-String 'depth='

# 4. 看最近 commit
git log --oneline -10
```

## 4. 關鍵決策、政策、可逆性

### 4.1 政策（master 已拍板，不要動）
- **中貴棋譜/ 822 本只 d12，不深算**（[render_site.py](site_builder/render_site.py) `PUBLIC_EXCLUDE_KEYWORDS`、[enrich_decisive.py](site_builder/enrich_decisive.py)）
- **公開 42 本 d22 全跑，但走到 |d12|>500 那步為止後跳過後續**（[enrich_decisive.py](site_builder/enrich_decisive.py) `DECISIVE_CUTOFF`）
- **d22 PV 存 [:10]，view 端 [:4]**；d28 master [:16]，view [:8]
- **d12 DFS 評估順序 + Hash 1024 MB，TT 不清空**（[D12_TT_SWEEP.md](D12_TT_SWEEP.md)）

### 4.2 不要做的事
- 不要 manual 啟用 `ChessBookVerifyD28Shunbao` 直到 d22 + d12 recompute 完成
- 不要刪 `output/.d12_dfs_recompute_done`（除非要強制重跑 d12 DFS）
- 不要動 d22/d28/d32 entries——它們不依賴 d12，d12 重算後仍有效
- 中貴棋譜 d22 sweep 已被排除，不要嘗試補

### 4.3 出問題的回滾路徑
- d12 recompute 跑壞：backup 在 `output/positions_pre_dfs_<ts>.js`（git 進 commit）。覆寫 → migrate → render
- 公開站 docs/ 壞：`git checkout HEAD~N -- docs/` 後 push

## 5. schtask 一覽

| schtask | 排程 | 狀態 |
|---|---|---|
| `ChessBookEnrichD22` | 22:30 daily, 8h | ✅ 進行中 |
| `ChessBookVerifyDepth28` | 21:00 daily | ✅ active (trap pair sweep) |
| `ChessBookVerifyD28Shunbao` | 22:30 daily | ⏸ disabled (d22 完後恢復) |
| `ChessBookVerifyD32` | 21:00 daily | ✅ active |
| `ChessBookNightlyBuild` | 一次性已過期 | 死碼，可忽略 |

## 6. 連接的姊妹 repo
- `D:\Elton\TestArea\chess-book-editor` — 讀 `output/positions.db` read-only
- positions.db schema 沒變，editor 完全不受本 session 變動影響

## 7. 待辦（未做、未急）
- d22 完成後恢復 `ChessBookVerifyD28Shunbao`
- 觀察 d12 DFS 重算後 trap 列表變化（686 → ?），看是否需要調整 `_compute_trap_stats` 門檻
- 若 positions_view.js d22 全跑完仍 < 50 MB，瘦身 A 階段足夠；否則考慮 per-game 拆檔（B 方案）
- **zip 已評估、不做（2026-06-02）**：GitHub Pages 傳輸層已自動 gzip（25.9 MB 檔案線上只下載 4.06 MB，`Content-Encoding: gzip`），手動 zip 對載入速度 0 改善；base64-gzip 塞 .js 反而讓下載變 ~7 MB（base64 不可再壓）+ 需 pako 解壓，真 .gz + fetch 又破壞 `file://`。50 MB 只是 warning（slim 後現為 25.9 MB，d22 補完估 ~31 MB，仍安全）。真逼近 100 MB 硬上限時走 **per-game 拆檔（B 方案）**，不要 zip。

## 8. 對話風格
- master 是 Traditional Chinese，terse，會推回不嚴謹的引擎輸出解讀
- 稱呼「尊敬的主人」（global instruction）
- 預設用 RTK prefix 給高噪音指令（git status/find/grep/log）
- d12 是「指標而非真理」，trap 真正裁判是 d22；不必為 d12 微動緊張
